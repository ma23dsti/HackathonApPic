name: hackathon_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - fpdf
  - flask
  - joblib
  - jsonschema
  - matplotlib
  - numpy
  - pandas
  - plotly
  - pytorch
  - scikit-learn
  - streamlit
  - pip:  # On a une erreur si on installe kaleido côté serveur avec conda.
    - kaleido
